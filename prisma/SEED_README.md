# 🌱 راهنمای استفاده از Seed Database

این فایل راهنمای استفاده از اسکریپت seed برای وارد کردن داده‌های اولیه به دیتابیس است.

## 📋 محتوای Seed

این اسکریپت داده‌های زیر را به دیتابیس وارد می‌کند:

### 🎯 دسته‌بندی‌ها (Categories)

- ✅ **ارزهای دیجیتال** (Cryptocurrency)
- ✅ **ایردراپ** (Airdrop)
- ✅ **متاورس** (Metaverse)
- ✅ **NFT**
- ✅ **بورس** (Stock Market)

### 🏷️ تگ‌ها (Tags)

- تحلیل تکنیکال
- تحلیل بنیادی
- بورس تهران
- ارز دیجیتال
- فارکس
- سهام بلندمدت
- صندوق‌های سرمایه‌ گذاری
- اوراق قرضه
- مدیریت ریسک
- تنوع سبد سرمایه‌ گذاری

### 📚 دوره‌های آموزشی (Courses)

- دوره تحلیل تکنیکال بورس
- دوره جامع ارزهای دیجیتال
- دوره تحلیل بنیادی بورس
- دوره NFT
- دوره متاورس
- دوره پیشرفته ترید کریپتو

### 💬 نظرات (Comments)

- نظرات تایید شده دانشجویان
- نظرات مربوط به هر دسته‌بندی

### 📖 کتاب‌های دیجیتال

- مجموعه‌ای از کتاب‌های آموزشی در حوزه سرمایه‌ گذاری

### 📰 اخبار و مقالات

- اخبار مرتبط با بازار

---

## 🚀 نحوه اجرای Seed

### مرحله ۱: اطمینان از اتصال به دیتابیس

ابتدا مطمئن شوید که فایل `.env` شما دارای `DATABASE_URL` صحیح است:

```env
DATABASE_URL="mongodb://..."
```

### مرحله ۲: اجرای Prisma Generate

```bash
npx prisma generate
```

### مرحله ۳: اجرای Seed

برای وارد کردن داده‌ها به دیتابیس، دستور زیر را اجرا کنید:

```bash
npx prisma db seed
```

**یا می‌توانید مستقیماً اسکریپت را اجرا کنید:**

```bash
node prisma/seed.js
```

---

## ⚠️ نکات مهم

1. **پاک‌سازی دیتابیس**: این اسکریپت قبل از درج، داده‌های قبلی را پاک می‌کند. پس مطمئن شوید که backup از دیتابیس دارید!

2. **ترتیب درج**: داده‌ها به ترتیب زیر درج می‌شوند:

   - تگ‌ها (Tags)
   - دسته‌بندی‌ها (Categories)
   - دوره‌ها (Courses)
   - نظرات (Comments)
   - کتاب‌ها (Books)
   - اخبار (News)

3. **روابط (Relations)**: اسکریپت به صورت خودکار روابط بین مدل‌ها (مثل `categoryId` و `tagIds`) را برقرار می‌کند.

4. **خطاها**: اگر خطایی رخ داد، پیام خطا در کنسول نمایش داده می‌شود.

---

## 🛠️ سفارشی‌سازی

برای اضافه کردن داده‌های بیشتر، فایل [seed.js](seed.js) را ویرایش کنید:

### اضافه کردن دسته‌بندی جدید:

```javascript
const categories = [
  // ... موارد قبلی
  {
    slug: "new-category",
    title: "عنوان جدید",
    description: "توضیحات...",
    // ...
  },
];
```

### اضافه کردن دوره جدید:

```javascript
const courses = [
  // ... موارد قبلی
  {
    subject: "موضوع دوره",
    price: 2000000,
    slug: "course-slug",
    // ...
  },
];
```

---

## ✅ بررسی نتیجه

بعد از اجرای seed، می‌توانید با Prisma Studio داده‌ها را بررسی کنید:

```bash
npx prisma studio
```

---

## 📊 آمار داده‌های Seed

| نوع داده     | تعداد             |
| ------------ | ----------------- |
| دسته‌بندی‌ها | 5                 |
| تگ‌ها        | 10                |
| دوره‌ها      | 6                 |
| نظرات پایه   | 4 (× 5 دسته = 20) |
| کتاب‌ها      | 9                 |
| اخبار        | 3                 |

---

## 🐛 رفع مشکلات رایج

### خطای "Table does not exist"

```bash
npx prisma db push
```

### خطای "Connection refused"

بررسی کنید که MongoDB در حال اجرا است و `DATABASE_URL` صحیح باشد.

### خطای Import

مطمئن شوید که `package.json` دارای `"type": "module"` است.

---

## 📞 پشتیبانی

در صورت بروز مشکل، به مستندات [Prisma Seeding](https://www.prisma.io/docs/guides/database/seed-database) مراجعه کنید.

---

**ساخته شده با ❤️ برای پروژه پیشرو**
