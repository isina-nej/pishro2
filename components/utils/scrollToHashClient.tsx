"use client";

import useScrollToHash from "@/lib/hooks/useScrollToHash";

const ScrollToHashClient = () => {
  useScrollToHash();
  return null; // این کامپوننت فقط برای اجرای هوک است
};

export default ScrollToHashClient;
