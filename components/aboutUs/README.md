# راهنمای صفحه درباره ما

صفحه درباره ما با دیزاین مدرن و زیبا بازسازی شد و شامل بخش‌های زیر است:

## 📋 بخش‌های صفحه

### 1️⃣ **Hero Section** (بخش اول)
- دیزاین مدرن با gradient و انیمیشن
- نمایش آمار کلیدی (دانشجو، دوره، رضایت)
- کاملاً responsive

### 2️⃣ **Resume Section** (رزومه نوشتاری)
شامل 4 کارت:
- **تاریخچه ما**: تاریخچه شروع فعالیت
- **ماموریت ما**: اهداف و ماموریت سازمان
- **چشم‌انداز ما**: برنامه‌های آینده
- **ارزش‌های ما**: اصول و ارزش‌های بنیادین

### 3️⃣ **Team Section** (بخش تیم)
- نمایش بانیان و اعضای کلیدی تیم
- اطلاعات تحصیلی و تخصص‌ها
- دکمه‌های ارتباطی (لینکدین و ایمیل)

### 4️⃣ **Certificates Gallery** (گالری تقدیرنامه‌ها)
- گالری زیبا با امکان نمایش تمام‌صفحه (Lightbox)
- نمایش تقدیرنامه‌ها و افتخارات
- انیمیشن hover جذاب

### 5️⃣ **Journals** (اطلاعیه‌ها و مقالات)
- لیست اطلاعیه‌ها و مقالات با دیزاین بهبود یافته
- کارت‌های مدرن با انیمیشن

### 6️⃣ **CTA Section** (فراخوان به اقدام)
- بخش تماس و دکمه‌های اقدام
- اطلاعات تماس (تلفن و ایمیل)

---

## 🖼️ راهنمای اضافه کردن تصاویر واقعی

### برای گالری تقدیرنامه‌ها:

1. تصاویر تقدیرنامه‌ها و گواهی‌های خود را در پوشه زیر قرار دهید:
   ```
   public/images/certificates/
   ```

2. فایل `certificatesGallery.tsx` را باز کنید و دیتای `certificatesData` را ویرایش کنید:

```typescript
const certificatesData = [
  {
    id: 1,
    image: "/images/certificates/certificate-1.jpg", // مسیر تصویر واقعی
    title: "تقدیرنامه واقعی", // عنوان واقعی
    description: "توضیحات واقعی", // توضیحات واقعی
  },
  // سایر تقدیرنامه‌ها...
];
```

### برای بخش تیم:

تصاویر اعضای تیم در حال حاضر از مسیر زیر استفاده می‌کنند:
- `/images/about/about.jpg` (دکتر طاهره جهانی)
- `/images/about/about3.jpg` (دکتر سید عنایت‌الله مومنی)

برای تغییر:
1. تصاویر جدید را در `public/images/about/` قرار دهید
2. در فایل `teamSection.tsx` مسیر `image` را تغییر دهید

### برای Hero Section:

آمارها و متن‌ها را مستقیماً در فایل `heroSection.tsx` ویرایش کنید:

```typescript
<div className="text-4xl font-bold mb-2">+۳۰۰۰</div> // تعداد دانشجو
<div className="text-4xl font-bold mb-2">+۱۰۰</div>  // تعداد دوره
<div className="text-4xl font-bold mb-2">%۹۵</div>   // درصد رضایت
```

---

## 🎨 تنظیمات رنگ و استایل

رنگ‌های اصلی پروژه:
- **Primary**: `myPrimary` (آبی)
- **Secondary**: `mySecondary` (نارنجی)
- **Accent**: `yellow-300` (زرد طلایی)

برای تغییر رنگ‌ها، فایل `tailwind.config.ts` را ویرایش کنید.

---

## 📱 Responsive Design

تمام بخش‌ها برای صفحه نمایش‌های مختلف بهینه شده‌اند:
- **موبایل**: `< 640px`
- **تبلت**: `640px - 1024px`
- **دسکتاپ**: `> 1024px`

---

## ⚡ انیمیشن‌ها

از کتابخانه `framer-motion` برای انیمیشن‌ها استفاده شده است:
- ورود از پایین (fade + slide up)
- انیمیشن hover
- انیمیشن scroll-triggered

---

## 🛠️ نکات فنی

### Dependencies مورد نیاز:
```json
{
  "framer-motion": "^11.x",
  "next": "^15.x",
  "react": "^19.x",
  "lucide-react": "^0.x" // برای آیکون‌ها
}
```

### کامپوننت‌های ساخته شده:
1. `heroSection.tsx` - بخش Hero
2. `resumeSection.tsx` - بخش رزومه
3. `teamSection.tsx` - بخش تیم
4. `certificatesGallery.tsx` - گالری تقدیرنامه‌ها
5. `ctaSection.tsx` - بخش CTA
6. `journals.tsx` - اطلاعیه‌ها (بهبود یافته)

---

## 🔄 به‌روزرسانی محتوا

برای تغییر محتوای متنی بخش‌های مختلف:

### تاریخچه، ماموریت، چشم‌انداز، ارزش‌ها:
فایل: `resumeSection.tsx`
متغیر: `resumeData`

### اطلاعات تیم:
فایل: `teamSection.tsx`
متغیر: `teamMembers`

### اطلاعیه‌ها:
فایل: `public/data.tsx`
متغیر: `pishroJournals`

---

## 📞 پشتیبانی

برای هرگونه سوال یا نیاز به تغییرات بیشتر، با تیم توسعه تماس بگیرید.
